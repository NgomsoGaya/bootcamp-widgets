document.addEventListener('alpine:init', () => {
    Alpine.data('greetingWidget', greetingWidget);
  });

document.addEventListener('alpine:init', () => {
    Alpine.data('transportWidget', transportWidget);
  }); 

document.addEventListener('alpine:init', () => {
    Alpine.data('wordgameWidget', wordgameWidget);
  });

document.addEventListener('alpine:init', () => {
    Alpine.data('airtimeWidget', airtimeWidget);
  });